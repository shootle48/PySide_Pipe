PySide6>=6.6.0
opencv-python>=4.9.0
numpy>=1.24.0
minimalmodbus>=2.1.0
pyserial>=3.5

# Dev / test
# pytest>=7.0

# On Jetson Orin Nano — do NOT pip install opencv-python
# Use system OpenCV instead:
#   sudo apt install python3-opencv
# Then install only:
#   pip install PySide6 numpy minimalmodbus pyserial

# Optional: Jetson GPIO trigger support (install on Jetson only)
# Jetson.GPIO>=2.0.0
